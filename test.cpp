#include <iostream>
using namespace std;

// Implement classes A-G here
// for each class write destructor and parameterized constructors
// use templates with class if necessary

// Write necessary operator overloading functions here (or inside class).
// use exception handling with op. overloading where necessary

int main()
{

    // only create non abstract classes, figure that out on your own
    // E<float> E1(...);
    // create an object of class E

    // A A1(...);
    // create an object of class A

    // B B1(...);
    // create an object of class B

    // C C1(...);
    // create an object of class C

    // G G1(...);
    // create an object of class G

    // G1.fb(); //make sure this works

    // G1.ff() //implement this or use appropriate delegate function

    // Then, make sure the following lines work, use appropriate techniques as necessary
    /*A * ptr;
    ptr = &B1;

    B1->display();

    double d1 = B1;*/
}
